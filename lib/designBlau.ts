/**
 * Design "Blau" – kraftvolles Ballsport-Theme (primary=#052856, SG-Navy).
 * Sprechender Alias für brandCss.ts – keine eigene Logik.
 */
export { CDN_CSS, cdnScript, MD3_COLORS, CDN_TW_CONFIG } from './brandCss'

/** Gemeinsame Hilfsklassen für alle Vereinsseiten (Impressum, Vorstand, Mitgliedschaft, Geschichte, Sponsoren). */
export const VEREINS_EXTRA_CSS = `
  .vanguard-gradient { background: linear-gradient(135deg, #052856 0%, #0a408a 100%); }
`
